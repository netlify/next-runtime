{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "outDir": "./lib" /* Redirect output structure to the directory. */,
    },
    "include": [
        "src/**/*.ts",
        "src/**/*.js"
    ],
    "exclude": [
        "src/templates/edge/*"
    ]
}