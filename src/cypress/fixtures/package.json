{
  "name": "next-on-netlify-test",
  "scripts": {
    "build": "next build",
    "preview": "netlify dev",
    "serve": "netlify dev --dir out --offline --port 9999",
    "predeploy": "mkdir -p .git",
    "deploy": "netlify deploy --json > deployment.json"
  }
}
